version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: pe-be-backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/pe_be
      - SECRET_KEY=${SECRET_KEY:-your-secret-key-here}
      - ENVIRONMENT=${ENVIRONMENT:-production}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - JOB_CHAT_ATTACHMENT_CLEANUP_ENABLED=${JOB_CHAT_ATTACHMENT_CLEANUP_ENABLED:-true}
      - JOB_CLOSE_STALE_OPEN_WORKOUTS_ENABLED=${JOB_CLOSE_STALE_OPEN_WORKOUTS_ENABLED:-true}
      - JOB_CLOSE_STALE_OPEN_WORKOUTS_MAX_AGE_HOURS=${JOB_CLOSE_STALE_OPEN_WORKOUTS_MAX_AGE_HOURS:-24}
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    volumes:
      - exercise_images_data:/app/.exercise_images
      - chat_attachments_data:/app/.chat_attachments
    networks:
      - pe-be-network

  db:
    image: postgres:17-alpine
    container_name: pe-be-postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=pe_be
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/init.sql:/docker-entrypoint-initdb.d
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - pe-be-network

  frontend:
    build:
      context: ./pe-be-tracker-frontend
      dockerfile: Dockerfile
    container_name: pe-be-frontend
    ports:
      - "3000:80"
    environment:
      - VITE_API_BASE_URL=http://backend:8000
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - pe-be-network

volumes:
  postgres_data:
  exercise_images_data:
  chat_attachments_data:

networks:
  pe-be-network:
    driver: bridge
