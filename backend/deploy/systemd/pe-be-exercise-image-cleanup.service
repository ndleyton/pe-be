[Unit]
Description=PE-BE exercise image cleanup
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
WorkingDirectory=/srv/pe-be
Environment=ENV_FILE=.env.production
ExecStart=/usr/bin/docker compose -f docker-compose.prod.yml run --rm -T backend python -m src.jobs.exercise_image_cleanup
